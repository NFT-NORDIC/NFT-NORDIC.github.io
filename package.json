{
  "name": "nftnordic_landingpage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider; nuxt",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider; nuxt build",
    "start": "export NODE_OPTIONS=--openssl-legacy-provider; nuxt start",
    "generate": "export NODE_OPTIONS=--openssl-legacy-provider; nuxt generate",
    "lint:js": "export NODE_OPTIONS=--openssl-legacy-provider; eslint --ext \".js,.vue\" --ignore-path .gitignore .",
    "lint": "export NODE_OPTIONS=--openssl-legacy-provider; npm run lint:js",
    "deploy": "export NODE_OPTIONS=--openssl-legacy-provider; push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "@babel/eslint-parser": "^7.14.7",
    "@nuxtjs/eslint-config": "^6.0.1",
    "@nuxtjs/eslint-module": "^3.0.2",
    "@nuxtjs/tailwindcss": "^4.2.0",
    "eslint": "^7.29.0",
    "eslint-plugin-nuxt": "^2.0.0",
    "eslint-plugin-vue": "^7.12.1",
    "postcss": "^8.3.5"
  }
}
